---
- name: Configure deployer user, hostname, and static IP (Rocky Linux)
  hosts: all
  gather_facts: yes
  become: yes

  vars:
    deployer_user: deployer
    deployer_pubkey: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHbyK5txNlRI6Q5ROalOv9UYnBV47VZNuFUrf6jbfmvY deployer@480"
    static_netmask: "24"
    static_gateway: "10.0.5.2"
    dns_servers: "10.0.5.2"

  tasks:

    # -------------------------------------------------------
    # 1. Add public key for deployer user
    # -------------------------------------------------------
    - name: Ensure deployer user exists
      ansible.builtin.user:
        name: "{{ deployer_user }}"
        shell: /bin/bash
        create_home: yes
        state: present

    - name: Add public key for deployer
      ansible.posix.authorized_key:
        user: "{{ deployer_user }}"
        state: present
        key: "{{ deployer_pubkey }}"

    # -------------------------------------------------------
    # 2. Sudoers drop-in for passwordless root elevation
    # -------------------------------------------------------
    - name: Create sudoers drop-in for deployer (no password)
      ansible.builtin.copy:
        dest: /etc/sudoers.d/deployer
        content: "deployer ALL=(ALL) NOPASSWD:ALL\n"
        owner: root
        group: root
        mode: "0440"
        validate: /usr/sbin/visudo -cf %s

    # -------------------------------------------------------
    # 3. Change hostname
    # -------------------------------------------------------
    - name: Set hostname
      ansible.builtin.hostname:
        name: "{{ new_hostname }}"

    - name: Update /etc/hosts with new hostname
      ansible.builtin.lineinfile:
        path: /etc/hosts
        regexp: '^127\.0\.1\.1'
        line: "127.0.1.1   {{ new_hostname }}"

    # -------------------------------------------------------
    # 4. Change DHCP to static IP via nmcli (Rocky/RHEL)
    # -------------------------------------------------------
    - name: Writing a static netplan config.
      ansible.builtin.shell:
      owner: root
      group: root
      mode: "0600"
      content: |
        network:
          version: 2
          renderer: NetworkManager
          ethernets:
            {{ network_interface }}:
              dhcp4: false
              addresses:
                - {{ static_ip }}/{{ static_netmask }}
              gateway4: {{ static_gateway }}
              routes:
                - to: default
                  via: {{ static_gateway }}
              nameservers:
                addresses:
                  - {{ dns_servers }}

    - name: applying configs.
      ansible.builtin.shell: netplan apply
      async: 10
      poll: 0